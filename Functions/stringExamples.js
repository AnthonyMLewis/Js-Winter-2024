// Declare a new string type variable with name sentences.
let sentence ='Yet another string primitive.';
console.log(sentence.charAt(14));

// Finding out if the sentence includes a word primitive
console.log(sentence.includes('another')); // return  true or false, if the word is present in sentence or not.

// Sentence to all upper case letters.
console.log(sentence.toUpperCase());

// Sentence to all lower case letters.
console.log(sentence.toLowerCase());

// Finding the length
console.log(sentence.length);


// Finding out which letter is stored in index 0
let letter = 'words';
console.log(letter.charAt(3));